const defaultState = {
  item: [],
  isFetching: true
};

export default function reposReducer(state = defaultState, action) {
  switch (action.type) {
    default:
      return state;
  }
}
